pipeline {
    agent any
    stages {
      stage('Tester maven') {
    steps {
        script {
                sh "mvn -version"   }
    }
      }
        stage('clean Maven') {
            steps{
                script{
                    sh "mvn clean"  }
            }
        }
    }
}
